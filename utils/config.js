// Set these once — every screen that talks to the backend reads from here.

export const API_BASE_URL = "https://smarttimetable-backend.onrender.com"; // <-- replace with your real Render URL
export const ADMIN_REGISTRATION_KEY = "adminaccessonlystudentnotallowedorgivenacess"; // <-- must match ADMIN_REGISTRATION_KEY in your backend's env vars